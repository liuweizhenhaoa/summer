server:
  port: 8080
  servlet:
    context-path: /es
tomcat:
  remote-ip-header: x-forward-for
  uri-encoding: UTF-8
  max-threads: 10
  background-processor-delay: 30
spring:
  http:
    encoding:
      force: true
      charset: UTF-8
  application:
    name: spring-cloud-study-security-elasticsearch
  data:
    elasticsearch:
      cluster-nodes: 168.13.0.241:9300
#      cluster-name: elasticsearch_liuwei  docker-cluster
      cluster-name: docker-cluster

